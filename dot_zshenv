#!/usr/bin/env bash
# zsh reads this for every shell (login, interactive, scripts); ~/.bashrc sources
# the same shared env directly. The body lives in ~/.config/sh/env.sh.
# shellcheck source=dot_config/sh/env.sh
. "${XDG_CONFIG_HOME:-${HOME}/.config}/sh/env.sh"
